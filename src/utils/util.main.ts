import { callfetch as fetchCall } from "./util.callfetch";
import { urlReport as endpoint } from "./util.endpoint";
import { getMenuItem as orderMenu } from "./util.getmenu";


export {
     fetchCall,
     endpoint,
     orderMenu,
};

